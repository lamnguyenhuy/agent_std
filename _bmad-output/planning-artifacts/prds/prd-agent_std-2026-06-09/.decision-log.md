# Decision Log: Agent Studio MVP PRD

Created: 2026-06-09

## Source Decisions Carried From Product Brief

1. Agent Studio is framed as a Git-native Agent Playbook system, not a generic prompt manager.
2. Primary message is "Review agent behavior changes like code."
3. Acquisition hook is loss of control -> restored control.
4. MVP wedge is repo-level behavior.
5. Primary navigation is Projects / Repos first.
6. Core primitive is Agent Playbook.
7. MVP Playbook shape is Skills, Agents, Rules, Context, and Tool Translators.
8. Workflows are deferred to v2.
9. MVP architecture is Git-native through `.agent-studio/playbook.yaml`.
10. External language uses Tool Translator, not Exporter.
11. Central aha moment is Cross-Tool Preview.
12. Review flow is Plan + simplified Behavior Diff + downloadable patch/local preview.
13. Actual GitHub PR creation is deferred to v1.1.
14. MVP uses sample repo/demo workspace first.
15. Demo repo is React/Next.js frontend.
16. Rules are editable first; skills, agents, and context are imported/read-only or lightly editable.

## PRD Authoring Decisions

1. PRD uses Fast path from the existing product brief and addendum.
2. PRD status remains `draft` because user validation and assumption triage are still pending.
3. Cross-cutting NFRs are included because this is a developer product prototype with reliability and terminology constraints.
4. Live GitHub PR creation remains explicitly out of scope.
5. Cursor output references `.cursor/rules` while retaining `.cursorrules` as compatibility/recognition context.

## Open Assumptions

1. Users will trust a Git-native Playbook as source of truth.
2. Cross-tool preview differences are enough to create an aha moment.
3. Patch/local preview is enough before GitHub PR integration.
4. Rules-only editing is enough to prove the core loop.
5. Sample repo validation is enough before arbitrary local repo support.

## Activation Notes

- `resolve_customization.py` failed because the available Python lacked `tomllib`; fallback used `customize.toml` defaults directly.
- No project-context.md persistent fact was found.
- No prior in-progress PRD was found under `_bmad-output/planning-artifacts/prds`.
